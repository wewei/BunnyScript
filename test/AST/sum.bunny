{
    sum = 0;
    {
        sum += a[i];
    }
    printf("%d", sum);
    if (sum > 100)
        if (sum > 1000)
            print("Beyound boundary"); 
        else
            printf("Large number");
}
        
